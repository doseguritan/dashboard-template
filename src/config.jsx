const config = {
    basename: '/',
    defaultPath: '/home',
    fontFamily: `'Roboto', sans-serif`,
    borderRadius: 3,
    gridSpacing: 3,
    drawerWidth: 260,
    appDrawerWidth: 320
  };
  export const {basename, defaultPath,fontFamily,borderRadius,gridSpacing,drawerWidth,appDrawerWidth} = config;
  export default config;
  